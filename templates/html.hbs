<!DOCTYPE html>
<html lang="tr">
<head>
	<meta charset="utf-8">
	<title>İşteTicaret Simgeleri</title>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
	<link rel="stylesheet" href="css/isteticaret-icons.css">
	<style>
		.icons { --bs-gutter-y: 1.5rem; }
		.icon-item { background-color: var(--bs-light); border-radius: .5rem; }
		.icon-item:hover { background-color: var(--bs-secondary); color: var(--bs-white); }
		.it, [class^="it-"], [class*=" it-"] { font-size: 3rem; margin: .5rem; }
		.label { font-family: var(--bs-font-monospace); display: inline-block; width: 100%; overflow: hidden; padding: .5rem; font-size: .75rem; text-overflow: ellipsis; white-space: nowrap; }
	</style>
</head>
<body>
	<div class="container-lg text-center">
		<div class="py-5 text-center">
			<div class="border-bottom mb-4 pb-4">
				<h1>İşteTicaret Simgeleri</h1>
			</div>
			<div class="row row-cols-4 row-cols-lg-5 row-cols-xl-6 icons">
			{{# each assets }}
				<div class="col">
					<div class="icon-item">
						<{{ ../tag }} class="{{ ../prefix }}-{{ @key }}" aria-hidden="true"></{{ ../tag }}>
						<div class="label">it-{{ @key }}</div>
					</div>
				</div>
			{{/ each }}
			</div>
		</div>
	</div>
</body>
</html>
